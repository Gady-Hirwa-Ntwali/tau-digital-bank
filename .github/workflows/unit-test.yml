name: first workflow unit test

on:
  push:
    branches:
      - 'feature/*'

jobs:
  run-unit-test:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run unit tests
        run: mvn test
